# 更新日志

**RichBar** 插件（基于原版 Emurasoft HTMLBar 的增强版 HTML + Markdown 工具栏）的所有重要变更均记录在本文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。版本号沿用本代码库历史沿革形成的项目专用方案：

| 版本区间 | 含义 |
|---------|------|
| `0.1.0`  | 原始上游 HTMLBar 源码原样 fork（Emurasoft `19.5.0` 合并版），未做任何修改。 |
| `0.4.x`  | 兼容性修复，使原插件能够在现代 EmEditor（v26）上正确编译和加载。 |
| `0.9.0` – `0.17.x` | HTML + Markdown 双模式工具栏，向 `1.0.0` 迈进。 |

## [0.21.5] - 2026-09-21

### 修复

- **自动刷新不再拉起外部浏览器**：`EI_OPEN_WEB` 文档虽写"web view"，
  实际打开默认浏览器（用户实测；探针没发现是因为已运行的 Edge 新开
  标签页不增加进程数）。重导航路径已移除。
- **面板检测改为存在性判断**（遍历不再要求 `IsWindowVisible`）：
  被其他面板标签页遮住、或位于另一框架下的面板也算"开"——这解释了
  之前按钮一直把面板读作不存在的那类会话。

### 变更

- **通往真正自动刷新的临时诊断**：预览面板窗口被子类化，收到的每条
  `WM_COMMAND` 都记入日志（自动刷新本体暂停——现在编辑正文只会保持
  子类就位，不做其他事）。在面板上手动右键 → 刷新一次即可捕获命令
  ID；下一版就用该命令实现实时同步。子类在面板销毁时自动卸除。

## [0.21.4] - 2026-09-21

### 变更

- **500ms 状态同步轮询已移除**（用户要求——一直不喜欢轮询）。面板状态
  全面改为推送驱动：预览面板关闭由 `EVENT_CUSTOM_BAR_CLOSED` 通知；面板
  窗口的出现/消失（包括从 EmEditor 自身 UI 打开——那不产生任何插件事件）
  由进程内 WinEvent 钩子（`EVENT_OBJECT_DESTROY..HIDE`，针对
  `EmEditorWebPreview2` 窗口类）捕获。两条路径都按面板真实可见性对账按
  钮。除编辑防抖外，唯一剩下的定时器是一次性 800ms 启动恢复——恢复完
  配置里的面板后自行注销。自测验证：切换面板时精确产生 `event sync`
  日志行，全程无定时器参与。

## [0.21.3] - 2026-09-21

### 修复

- **预览自动刷新改为官方 API 实现**：0.21.2 向面板 WebView2 窗口投递
  `VK_F5` 的做法根本到不了 WebView2 的浏览器快捷键层——刷新从未发生。
  现在改用 `EI_OPEN_WEB` 把面板重新导航到 WebPreview 插件自己使用的
  渲染页 URL（`file:///…/PlugIns/markdown-renderer.html?documentName=…
  &documentFolder=…`，按同样规则百分号编码）；每次导航都会重载渲染页，
  渲染页随即从服务实时缓冲区的虚拟主机重新抓取文档。无标题文档跳过
  （其快照名无法重建），刷新也绝不会把面板顺手打开。自测验证：
  `EI_OPEN_WEB` 返回成功、面板数量保持一个、不拉起外部浏览器。
- **设计视图“要按两次”修复——不再信任 407**：追踪日志显示
  `EI_GET_MARKDOWN_PREVIEW` 又慢又抖（按文档区分、受标签页/面板状态影
  响——两秒内 1→0→1 来回跳），500ms 轮询不断拿这个噪声“纠正”按钮，
  每次点击半秒后被撤销、视图被来回切。已移除全部 407 用法：按钮改为
  诚实的本地开关（一次点击 = 一次 `EEID_MARKDOWN_VIEW`），配置标志 +
  无条件启动恢复是唯一的记忆来源。

## [0.21.2] - 2026-09-21

### 新增

- **预览实时同步**：正文每次修改都会重置一个 400ms 防抖计时器，停顿时对
  可见的预览面板发送一次 `VK_F5` 重载。WebPreview 渲染页在每次页面加载时
  都会从插件的虚拟主机重新抓取文档（虚拟主机服务实时缓冲区），因此一次
  重载即完成同步——效果等同面板右键的“刷新”，但无需手动操作。

### 修复

- **设计视图按钮状态**：启动恢复不再等待 `EI_GET_MARKDOWN_PREVIEW`
  曾经返回过 TRUE——那道门槛导致“会话启动时视图是关着的”情形下保存的
  开启状态永远不恢复、轮询也永远不生效（EmEditor 自身不持久化设计视图，
  已用注册表运行时差分证实，我们的配置标志就是唯一的记忆）。两个开关
  按钮点击时也先对账再发命令：内置命令是切换语义，只有实际状态与期望
  不一致时才发 23255/23275——对已对齐按钮的点击不会再把面板关掉或把
  视图翻反。

## [0.21.1] - 2026-09-21

> 注：中文日志 0.18.0–0.21.0 期间未同步，详请见英文 `CHANGELOG.md`。

### 变更

- **预览按钮两种模式统一走官方命令 `EEID_MARKDOWN_PREVIEW`（23275）**，
  彻底移除对 `WebPreview.dll!OnCommand` 的直连调用。核心命令始终把
  WebPreview 插件绑定到**当前活动视图**——我们的直连可能把切换标签页
  之前缓存的旧视图交给插件，导致面板绑错文档、右键刷新永远刷出旧内容。
  查证（受控探针 + WebView2 历史取证）同时证明：配置在 WebPreview 的
  Markdown 列表内时，23275 会完整转换并渲染 Markdown。
- **预览按钮的按压态改为跟随面板实际可见性**（在框架下枚举插件的
  `EmEditorWebPreview2` 窗口），不再依赖记忆标志位，与设计视图按钮信任
  `EI_GET_MARKDOWN_PREVIEW` 的做法同源；自己点击后短暂暂停同步，等待
  WebView2 异步开关面板。
- **模式切换现在同步设置文档配置**（`[M]` → "Markdown"，`[H]` →
  "HTML"）：WebPreview 按配置名选择渲染管线（仅 Markdown 配置的文档走
  转换），模式切换即保证预览类型正确。仅手动点击时设置，自动检测仍然
  只读取配置。

## [0.17.5] - 2026-09-19

### 修复

- **0.17.1-0.17.4 的全部下拉箭头尝试均已撤回**（master 上的 revert 提交）；
  工具栏代码恢复为久经验证的 0.15.7 架构——`BTNS_WHOLEDROPDOWN` 按钮
  由控件自绘主题箭头、热态图像列表与按下态深色副本齐备——仅将数据源
  指向 Remix 子集及其码位。
- [H][M] 模式开关绘制 Remix 的 `html5-fill`/`markdown-fill` 字形；
  旧版 Markdown 字母/形状回退绘制与 HTML `?` 标记（Remix 字形补齐后
  已不可达）一并移除，字体路径成为唯一的图形来源。Remix 子集（62 个
  去重字形，8,964 字节，SHA256 ea113337d0b9b4cb0f645f13a2e1333e0fa86884f057bb30903cc0696b3d708a）
  现已包含这两个模式字形。

### 变更

- `tools/test-icon-rendering.ps1` 随架构恢复（热态列表与按下副本回归），
  基准表与字体名校验更新为 `remixicon` 码位。

## [0.17.0] - 2026-09-18

### 变更

- **[H][M] 模式开关改用 Remix Icon 图案**，单一图标来源就此完整：HTML
  显示 `html5-fill`（U+EE40），Markdown 显示 `markdown-fill`（U+EF1D），
  均选用填充变体以保证工具栏尺寸下的清晰度。原 Segoe UI Bold 字母绘制
  （`DrawMdText`、`MD_TEXT_HEIGHT`）已删除——工具栏上的所有按钮都绘制
  自同一个内嵌子集。
- 内嵌子集扩展到 **61 个去重图标**（8,712 → **8,924 字节**，新 SHA256
  已记录于 `LICENSE.third-party` 与 `docs/remix-icon.md`）；
  `tools/remix-icons.json` 新增 `modes` 数组，将两个开关字形纳入子集。

## [0.16.0] - 2026-09-18

### 变更

- **内嵌图标字体由 Lucide 更换为 Remix Icon，两种按钮集统一换源。**
  内嵌的 `lucide_subset.ttf` 已移除，替换为 **Remix Icon 4.9.1** 的子集
  `remixicon_subset.ttf`（字体族名 "remixicon"，**59 个去重图标，
  8,712 字节**）。Markdown 与 HTML 两套集合共用这一单一来源；H1–H6 改用
  Remix 的 `h-1`–`h-6` 图形。`tools/subset-icon-font.cjs` 依据仓库内的
  `tools/remix-icons.json` 名称表，从 Remix Icon 字体目录重新生成子集，
  并在上游版本不符时拒绝运行。
- `LICENSE.third-party` 现在完整逐字收录 Remix Icon License v1.0
  （Lucide/Feather 声明已删除——仓库中已无 Lucide 代码）。
  `docs/remix-icon.md` 取代 `docs/lucide-font.md`。

### 移除

- **全部回退图形。** 旧版 Markdown 字母/形状绘制与 HTML 加粗 `?` 标记
  均已删除：字形无法解析（字体注册失败或字形不可用）的槽位现在保持
  **空白**。这是明确的简化——字体随 DLL 一起发布。随之删除的还有不再
  使用的 `DrawMdHeading` / `DrawMdTextAt` 辅助函数与 `MD_CODE_HEIGHT` /
  `MD_SUBSCRIPT_HEIGHT` 常量。

### 修复

- 图标回归测试套件同步新设计：回退场景断言已映射槽位保持**空白**
  （而非非空）。Markdown 图标覆盖改为与直接 Remix Icon 绘制比对
  （7 种尺寸 × 2 种前景色共 280 项像素一致比较）；两种模式的真实普通
  与热态图像列表（含 H/M 槽位与按下态深色副本）继续以每场景 1,464 项
  比较覆盖。字体注册/释放/重新初始化检查保持不变。

## [0.15.7] - 2026-09-17

### 变更

- **提示先行、菜单殿后的分层时序。** 悬停下拉按钮先显示工具提示；只有
  鼠标继续停留才展开菜单。悬停延迟取系统提示初始延迟（`TTM_GETDELAYTIME`
  的 `TTDT_INITIAL`）加 500 毫秒阅读余量（无提示控件时回退 1 秒）；菜单
  弹出时经 `TTM_POP` 收起提示，两个弹出层不再重叠。普通悬停不受影响，
  下拉按钮上的工具提示终于能正常看到了。

### 修复

- **撤回 0.15.6 的空提示方案。** 让 `TTN_GETDISPINFO` 返回空文本是糟糕的
  变通：提示控件仍可能弹出空提示框，紧贴按钮的空框看起来就像图标轻微
  位移，且下拉按钮的工具提示依旧不可用。处理函数已恢复为原样提供标题。

## [0.15.6] - 2026-09-17

### 修复

- **0.15.5 之后工具提示几乎永久消失。** 用停用提示控件的方式给菜单让位，
  在鼠标从未离开按钮的情况下会让控件休眠：缺少新的"工具进入"事件，显示
  计时器不再启动，提示就此永久沉默。现在菜单让位不再触碰控件状态：菜单
  打开时用 `TTM_POP` 隐藏已显示的提示，菜单跟踪期间 `TTN_GETDISPINFO`
  一律返回空文本（空提示不会显示）。提示控件自身的生命周期不被打断，
  菜单关闭后鼠标一动提示即正常恢复。

## [0.15.5] - 2026-09-17

### 修复

- **悬停展开的子菜单会盖住按钮的工具提示。** 菜单与工具提示都是置顶
  弹出层，且争夺按钮下方同一位置；Windows 的标准做法是菜单跟踪期间
  工具提示让位（菜单栏与 Ribbon 亦如此）。现在弹出下拉菜单前先隐藏
  工具栏的提示控件（`TTM_POP`）并临时停用（`TTM_ACTIVATE`），菜单关闭
  后恢复——提示在下一次鼠标移动时照常出现，且永远不会与菜单重叠。

## [0.15.4] - 2026-09-17

### 修复

- **展开菜单时按钮图标轻微位移。** 按下态（0.15.3 用它保持高亮）会让
  工具栏把按下图标按经典的 1 像素右下偏移绘制。悬停路径不再按下按钮：
  现已子类化工具栏控件，菜单跟踪期间吞掉 `WM_MOUSELEAVE`，热态外观自然
  不会消退，图标也纹丝不动。点击路径不受影响。
- **反复开关两三次后悬停不再弹出。** 0.15.2–0.15.3 的机制依赖
  `TBN_HOTITEMCHANGE`，而模态菜单前后该通知的时序并不可靠。现在整套
  机制改为在子类中直接处理鼠标事件：`WM_MOUSEMOVE` 上用 `TB_HITTEST`
  决定是否启动悬停计时器，而"鼠标已离开上次弹菜单的按钮"——允许再次
  弹出的条件——由鼠标移动的命中变化与 `WM_MOUSELEAVE` 本身跟踪，
  不会再丢失。

## [0.15.3] - 2026-09-17

### 修复

- **悬停打开菜单期间按钮高亮会消退。** 悬停路径弹出菜单时按钮未处于按下
  态，菜单循环捕获鼠标后工具栏热态跟踪超时，按钮退回普通外观。现在悬停
  路径同样在菜单期间将按钮保持按下态（并应用 0.15.1 的深色字形交换），
  关闭后恢复，观感与点击路径一致。
- **点空白关闭菜单后悬停不再弹出。** `TBN_HOTITEMCHANGE` 会把一次鼠标
  移动的"离开 A + 进入 B"合并为一条通知，而处理函数在离开分支就返回，
  吞掉了之后的所有悬停；同按钮抑制也只依赖可能丢失的离开事件。现在离开
  与进入各自独立处理，抑制额外按双击时长超时兜底，绝不会永久卡住。菜单
  跟踪期间忽略热项扰动与遗留计时器，避免重入弹出两层菜单。

## [0.15.2] - 2026-09-17

### 新增

- **下拉菜单悬停自动展开。** 鼠标移到标题、字体或窗体按钮上，按系统菜单
  延迟（`SPI_GETMENUSHOWDELAY`）自动显示菜单，如同菜单栏——无需点击，
  也没有按下态重绘。移到普通按钮上会取消待触发的悬停展开；键盘热项导航
  不会自动展开。菜单刚关闭的按钮会保持安静，直到鼠标移出后再进入，避免
  关闭菜单瞬间重新弹出。点击下拉按钮仍然有效：该路径保留 0.15.1 的
  按下态深色字形交换。三个下拉处理已合并为点击/悬停共用的
  `ShowDropdownMenu`。

## [0.15.1] - 2026-09-17

### 修复

- **深色栏上字体（"T"）按钮悬停反色异常。** 该按钮此前使用分裂式
  `BTNS_DROPDOWN` 样式：悬停在其小箭头区域会触发按压态绘制，而按压态
  始终取用普通图像列表——浅色字形压在浅色悬停填充上，看起来就像没有
  反色，其余按钮则正常。字体按钮现改为整钮下拉（与标题、窗体按钮一致）；
  菜单中仍保留打开字体对话框的 `Font...` 项。
- **下拉按钮菜单打开期间颜色错误。** 按下状态的按钮始终用普通图像列表
  绘制，深色栏上的窗体/标题/字体按钮在按下期间会呈现浅底浅字。现在普通
  列表尾部追加了每个图像的深色副本（与热态列表一致），下拉菜单弹出期间
  仅将这一颗按钮经 `TB_SETBUTTONINFO` 指向其深色副本，菜单关闭后恢复。
  其余按钮保持正常字形；浅色栏行为不变（深色字形本就可读）。自定义对话框
  的图标选择器只枚举浅色图标，已保存的图标槽位不受影响。

## [0.15.0] - 2026-09-17

### 变更

- **HTML 工具栏的旧版彩色 BMP 图标替换为与 Markdown 集合一致的内嵌
  Lucide 设计。** 全部 48 个持久化 HTML 槽位（25 个默认按钮，加上 23 个
  仅自定义对话框可见的命令，如表单控件、媒体符号）现在通过同一条共享
  字形路径绘制运行时单色 Lucide 图标：两种模式共用一套笔画语言，跟随栏
  背景亮度 / Very Dark 模式，任意 DPI 下清晰。已保存的图标槽位、自定义
  配置和命令行为不变——只有图形不同。
- 内嵌字体子集从 20 个扩展到 **58 个去重 Lucide 图标**（7,780 →
  **21,564 字节**）以覆盖 HTML 集合。子集现在可复现：
  `tools/subset-lucide.cjs` 依据仓库内的 `tools/lucide-icons.json` 名称表，
  从 `lucide-static` 字体目录重新生成，并在上游字体版本不符时拒绝运行。
  `LICENSE.third-party` 与 `docs/lucide-font.md` 记录了新产物（含 SHA256）。
- **悬停重色现覆盖 HTML 模式。** 两种集合均为单色字形后，热态图像列表会
  以深色字形重建每个 HTML 按钮，保证浅色悬停填充下可读（此前彩色 BMP
  直接复制、不重色）。插件列表中的插件入口图标保留原位图。
- 若某个 HTML 槽位的字形不可用（字体注册失败），按钮绘制加粗 `?` 标记，
  不显示错误图标；Markdown 保留旧版字母/形状回退。0.14 期间临时禁用全部
  Markdown 回退的诊断改动已在本次发布前还原。

## [0.14.0] - 2026-09-17

### 变更

- 当前 Segoe 图标字体映射改为来自 `lucide-static` 1.47.0 的内嵌 Lucide
  子集，覆盖全部 20 个 Markdown 命令图标（索引 0–19），包括 H1–H6、
  代码块、有序列表与水平线。README 与 `docs/lucide-font.md` 列出了完整
  名称及码位映射。
- 将 7,780 字节的 `lucide_subset.ttf` 作为 RCDATA 资源内嵌，通过
  `AddFontMemResourceEx` 直接从内存加载，仅对当前进程可用：不安装系统
  字体、不创建临时字体文件，也不依赖已安装的 Segoe 图标字体。字体加载
  失败时仍回退为旧版字母/形状绘制。
- 普通/大图标画布保持 96 DPI 下 16/24 px，随显示 DPI 缩放，不额外放大。
  H/M 仍使用 Segoe UI Bold，96 DPI 下为 14/21 px；HTML 工具栏的原版
  彩色 BMP 资产保持不变。
- `docs/glyph-reference.html` 仅保留为历史 Segoe 参考，其中的高亮映射
  不再代表当前工具栏。
- 明确构建与部署分离：`tools/build-loc-2052.ps1` 现已置于仓库内部，
  只负责构建。手动部署到仓库同级 `../dist/`，包含主 DLL、`1033`/`2052`
  两个卫星 DLL，以及位于主 DLL 旁的 `LICENSE.third-party`。

### 新增

- `LICENSE.third-party` 完整逐字保留本地上游许可：2026 Lucide ISC 许可、
  Feather 衍生图标列表及 Cole Bemis 的 MIT 署名与许可。
  `docs/lucide-font.md` 记录包来源、子集名称/映射、字节数及 SHA256；
  不声称仓库已包含可复现的子集生成脚本。
- `tools/test-icon-rendering.ps1` 现已覆盖全部 20 个 Lucide 图标、多种
  尺寸与颜色（与直接 Lucide 绘制的 280 项像素一致比对），并新增模拟
  字体不可用与字形缺失的回退子进程，以及字体注册/释放/重新初始化检查。
  这属于离屏测试覆盖，不等于 EmEditor 界面验证。

### 修复

- 每个 Lucide 图标绘制前都会校验解析出的字体名与字形索引，任一失败即
  回退为字母/形状绘制；字体注册失败不会被缓存，下次绘制会自动重试。
  注册在插件正常 `EVENT_CLOSE` 关闭时释放（绝不在 `DllMain` 中），
  之后若再次绘制图标会按需重新注册。

## [0.13.4] - 2026-09-17

### 变更

- 运行时绘制的 Markdown 图标不再叠加 135% 放大。96 DPI 下按图标画布尺寸
  渲染：普通 16 px、大图标 24 px，并随显示 DPI 缩放。文字按钮使用
  Segoe UI Bold，以 16 像素画布为基准的固定像素高度：H/M 字母 14/21 px
  （普通/大图标），标题数字 8/12 px 并位于 H 的右下角，代码块文字
  12/18 px。标题按钮将 H 与数字分开绘制，并按组合墨迹范围整体居中；
  不再用同一字号的字体绘制"H1"。

### 修复

- 将 Markdown 图标字体选择器初始化为实际的“未找到字体”状态，使字体探测
  能够执行。此前初值为 -1，但探测循环要求 0，导致所有已映射图标均使用
  旧版绘制，包括链接（E71B）。此前声称工具栏已经使用这些字体映射的说明
  不正确。本次不改变码位映射。
- 新增 `tools/test-icon-rendering.ps1`：从 RichBar.h 提取绘制方法进行编译，
  在 16/24/32 像素及两种前景色下，将链接图标与 Fluent E71B、旧版回退图形
  做像素对照。这是离屏回归测试，不是 EmEditor 界面测试。

## [0.13.3] - 2026-09-17

### 新增

- **交互式字形参考**：`docs/glyph-reference.html`——列出两套系统图标字体
  暴露的全部字形（经 GDI 探测发现 Fluent 2033 个、MDL2 1833 个，多于官方
  页面文档化的数量），基于本机安装的字体实时渲染，带 Light/Dark 配色与
  字体切换、过滤功能，并以蓝色边框标出 RichBar 当前映射的字形。
  可用 `tools/generate-glyph-reference.ps1` 重新生成。

### 修复

- **表格按钮现在绘制真正的网格字形**（Segoe Fluent Icons 的 U+F232
  GridViewSmall），而不是圆环。这一错误是通过本地枚举字体发现的——解析
  SegoeIcons.ttf（1213 个码位）与 SegMDL2.ttf（1103 个）的 cmap 表拿到了
  完整字形集（含未文档化条目）：官方确认 EA3A 是 CircleRing 而非 Table，
  EA1C 则根本不存在。U+F232 不在 Windows 10 的 MDL2 Assets 中，
  该系统上按钮自动回退为形状绘制的网格。

## [0.13.2] - 2026-09-17

### 修复

- **对照官方 Fluent/MDL2 字形表审计时发现错误的字形映射。** `EA3A` 是
  `CircleRing`（圆环）而非表格（此前"表格"按钮画出的是圆环），`EA1C` 在
  两套字体中均不存在。表格与有序列表按钮现在固定使用原有的形状绘制
  （网格 / 数字）；其余十个字体字形已逐一对照 Microsoft Learn 官方字形表
  核实，README 增加了完整的图标对照表（含两套字体的预览图），
  并形成中英文双语文档对（README.md / README.zh.md）。

## [0.13.1] - 2026-09-17

### 变更

- **字形尺寸旋钮从 115% 提高到 135%**——字母类图标（H1–H6、B/I/S、`[H][M]`
  开关）与官方工具栏相比仍显得偏小。

## [0.13.0] - 2026-09-16

### 新增

- **Markdown 图形类图标改用系统图标字体绘制**（Windows 11 为 Segoe Fluent
  Icons，Windows 10 为 Segoe MDL2 Assets）：粗体、斜体、删除线、行内代码、
  引用、无序/有序/任务列表、链接、图片、表格、齿轮全部换成真正的图标设计，
  不再是字母/形状近似。每个字形在每台机器上做一次可用性校验；没有字体字形
  的图标保留原有字母/形状绘制（标题 H1–H6、代码块、水平线和 `[H][M]` 开关
  本身就按字母/形状设计），没有这些系统字体的机器整体回退。
  插件不携带任何字体文件。

## [0.12.2] - 2026-09-16

### 变更

- **运行时绘制的字形放大 15%**（统一缩放旋钮 `GLYPH_SIZE_SCALE`，可继续调整），
  以更好地匹配官方工具栏的图标尺寸。

## [0.12.1] - 2026-09-16

### 修复

- **深色带区模式下，鼠标悬停按钮时字形被"洗白"。** 悬停时工具栏控件会用系统的
  浅色高亮填充按钮，但继续从同一张图像列表取图，浅色字形就成了浅底浅字。
  现在深色带区时会额外给工具栏挂一张**热态图像列表**（`TB_SETHOTIMAGELIST`）
  ——正常列表的深色字形镜像——悬停按钮渲染为浅底深字，清晰可读。

## [0.12.0] - 2026-09-16

### 新增

- **Very Dark（极暗）模式适配。** EmEditor 的 Very Dark 主题会把整个栏区域画成
  黑色，但它自己的工具栏图标在那里并不翻转为对比色。RichBar 现在使用官方的
  极暗模式插件 API（常量取自 v20.5 SDK）：极暗模式激活时 `EI_IS_VERY_DARK`
  把运行时绘制的字形切换为浅色，`EI_WM_CTLCOLOR` 用 EmEditor 自己的画刷把
  栏的对话框背景融入黑色带区，`EI_WM_THEMECHANGED` 在主题切换时重绘图标。
  没有这些消息的旧版 EmEditor 自动回落到 0.11.2 的背景亮度逻辑。

## [0.11.3] - 2026-09-16

### 修复

- **工具栏在"查看 > 工具栏"菜单中没有名称。** 该菜单条目复用自定义栏的栏标题，
  而 0.11.1 为了节省空间把它清空了。现在栏标题改为常量 "RichBar"——不再随模式
  变化（模式由 `[H][M]` 开关表达），工具栏在菜单中可以识别，
  屏幕上的标签也不再是冗余的模式文本。

## [0.11.2] - 2026-09-16

### 修复

- **工具栏字形（Markdown 图标集和 `[H][M]` 开关）颜色太淡。** 字形颜色现在
  跟随栏区域的真实背景：向 EmEditor 查询栏背景色，按其亮度决定使用深色字形
  （浅色栏，`#303030`）还是浅色字形（深色栏，`#E0E0E0`）；查询不到时按浅色
  处理——因为 EmEditor 的深色主题目前仍把栏区域留成浅色。之前字形跟随的是
  上报的栏*文字*色，那是为深色栏设计的浅色，画在浅色栏区域上就被"洗淡"了。
- 当检测到的字形颜色变化时（主题或配置切换）自动重建工具栏，
  绘制的图标无需重启即可跟随配色方案变化。

## [0.11.1] - 2026-09-16

### 变更

- **去掉了工具栏上的 "HTML"/"Markdown" 文本。** 工具栏上的 `[H][M]` 开关已经
  能够表明当前模式，栏标题文本属于冗余；整条宽度现在全部让给按钮。
  （标题作为模式指示是 0.10.3 恢复的，0.11.0 加入的开关已将其取代。）

## [0.11.0] - 2026-09-16

### 新增

- **手动模式切换：工具栏最左侧的 `[H][M]` 分段按钮。** 最左侧两个按钮构成
  标准的成组切换——按下的一侧是当前模式（H = HTML，M = Markdown）。点击任意
  一侧会立即切换按钮集、栏标题和按下状态。这覆盖了新建未保存文档的场景：
  此时既没有文件名也没有匹配的配置可供自动检测。手动覆盖在文档状态保持不变
  期间有效；任何文档或配置变化都会让工具栏回到自动检测——自动检测仍是默认行为。

## [0.10.3] - 2026-09-16

### 修复

- **恢复了自定义栏标题中的模式指示。** 栏标题再次根据当前模式显示 "HTML" 或
  "Markdown"（切换文档或配置时，标题随按钮集一起更新）。此前更名时把标题
  统一成了 "RichBar"，丢失了这个指示。标题字符串现在取自卫星资源
  （`IDS_TITLE` / 新增 `IDS_TITLE_MD`），不再硬编码。RichBar 品牌名保留在
  插件名称中。

## [0.10.2] - 2026-09-16

### 修复

- **插件版本列显示为陈旧的字符串 "14"。** EmEditor 的版本列来自插件自己的
  `EP_GET_VERSION` 字符串（卫星中的 `IDS_VERSION`），而不是文件本身的
  VERSIONINFO 资源，该字符串自 fork 原版 HTMLBar 起就一直未被改动。
  现在该字符串跟随发布版本更新。在卫星无法加载的时期（见 0.10.1），
  EmEditor 会静默退回读取 VERSIONINFO 资源，因此这个问题一直被掩盖。

## [0.10.1] - 2026-09-16

### 修复

- **插件列表中的名称显示为空白。** 两个独立原因，均已修复：
  - etlframe 解析插件字符串所用的卫星实例直到第一个框架创建（`EVENT_CREATE_FRAME`）
    后才初始化，而插件扫描阶段（尚无任何框架）发出的名称查询会退回到
    不含任何字符串的主 DLL。现在 `EEGetLocaleInstanceHandle` 会按需加载卫星
    （加载的实例缓存在 `m_hinstLoc` 中，随正常关闭流程释放），
    插件名称在扫描阶段即可正确解析。
  - EmEditor 按界面语言的资源标签精确匹配卫星资源：`mui\2052` 中的文件必须打上
    `LANG_CHINESE`（0x804）标签，而不能是英文标签（0x409）二进制的原样拷贝。
    卫星资源脚本现在通过 `LOC_LANG_2052` 预处理宏选择资源语言，
    `tools/build-loc-2052.ps1` 负责构建 `mui\2052` 版本。

## [0.10.0] - 2026-09-16

### 变更

- **插件全面更名为 RichBar。** 所有工程文件（`RichBar.sln`、`RichBar.vcxproj`、
  `RichBar.cpp/.h/.rc/.def`）、多语言资源工程（`mui/RichBar_loce`，产出 `RichBar_loc.dll`）、
  EmEditor 中显示的插件名与状态栏文本、自定义栏标题、属性对话框标题，
  全部由 HTMLBar 更名为 RichBar。
- **设置存储完全独立。** 插件 Profile 键名派生自插件 DLL 文件名，因此更名后
  设置存储从 `EmEditorPlugIns\HTMLBar` 迁移到全新的 `EmEditorPlugIns\RichBar` 键。
  新键不会与原版 HTMLBar 插件或任何同名插件发生冲突，同时仍然走官方的
  `EE_REG_SET_VALUE`/`EE_REG_QUERY_VALUE` 通道（注册表；INI 模式下自动改用 `eePlugins.ini`）。
- 自定义栏标题在两种模式下统一显示为 "RichBar"（当前按钮集本身即可表明所处模式）；
  之前显示 "HTML"/"Markdown"。
- 版本编号从最初拟用的 0.9.1 改为 0.10.0——更名的改动规模超出了一个补丁版本的范畴。

### 移除

- 移除了将旧 HTMLBar 布局保存的单数组按钮数据（`CmdArray`）自动迁移的逻辑——
  RichBar 是全新插件，从零开始。
- 移除了从原版 HTMLBar 安装器继承的 MSI 卸载集成
  （读取 `HKLM\...\EmEditorPlugIns\HTMLBar` 中 ProductCode 的逻辑）；
  卸载现在使用标准的"确认后删除插件设置"流程。

## [0.9.0] - 2026-09-16

### 新增

- **Markdown 模式。** 工具栏现在携带两套完整的命令集（HTML 和 Markdown），并自动切换：
  模式依据当前配置名检测（Markdown / HTML 配置名列表相互独立，且跨会话持久保存），
  并以文件扩展名兜底（`.md` `.markdown` `.mdown` `.mkd` 与 `.htm` `.html` `.xhtml` `.shtml`）。
  切换文档或配置时，自定义栏会以对应按钮集自动重建。
- **Markdown 按钮集**（25 个平铺按钮）：H1–H6 标题、粗体、斜体、删除线、行内代码、
  围栏代码块、引用、无序列表、有序列表（自动编号）、任务列表、水平线、链接、
  图片（带文件选择器）、Markdown 表格（复用行列对话框）、以及自定义按钮。
- 新增 `CMD_LINE_PREFIX` 命令类型：为选区各行添加/取消行前缀（标题、引用、列表），
  切换样式时自动剥离冲突的 Markdown 前缀，有序列表自动编号。重复点击同一前缀即取消。
- 包裹类命令支持切换语义：对已被包裹的文本再次点击粗体（及所有 `CMD_TAGS` 命令）
  将移除标记，而不是嵌套。
- **运行时绘制的 Markdown 图标**：Segoe UI 字形按当前按钮尺寸直接绘制在透明背景上，
  颜色取自编辑器的栏文字色（`EI_GET_BAR_TEXT_COLOR`）。任意 DPI 下都保持清晰，
  并与深色/浅色主题自然融合。
- 自定义栏标题现在反映当前模式（"HTML" / "Markdown"）。

### 变更

- 命令集按模式分别持久化（插件 Profile 中的 `CmdArray0` / `CmdArray1`）；
  早期版本写入的单数组数据会自动迁移到 HTML 按钮集。

## [0.4.0] - 2026-09-15

### 修复

- **现代 EmEditor 上工具栏以零宽度挂载。** 工具栏控件带有 `CCS_NORESIZE` 样式，
  导致其窗口始终保持初始的 0 像素宽度；而 EmEditor v26 在挂载时只测量一次客户窗口尺寸，
  把自定义栏定成仅有标题的宽度，最终渲染出一条空栏（只有"HTML"字样、没有任何按钮）。
  现在会在调用 `Editor_ToolbarOpen` 之前通过 `TB_GETMAXSIZE` + `MoveWindow`
  显式撑开窗口，并改为发送理想宽度，不再恢复过期的旧 band 宽度。
- **插件列表中版本号为空。** 工程从未嵌入 `VERSIONINFO` 资源；现已补上，
  插件从此可以像其他插件一样正常报告版本号。
- **MUI 资源 DLL 编译失败**（`致命错误 RC1015: 无法打开包含文件 'afxres.h'`）：
  资源脚本改用 `winres.h`，移除了隐藏的 MFC 依赖。

## [0.1.0] - 2019-12-19

### 基线

- 从 Emurasoft fork 而来的原始 HTMLBar 插件源码（`19.5.0` 版本合并），
  未做任何修改。这是之后所有版本演进的对照基准。
